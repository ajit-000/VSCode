#include<stdio.h>

int add_sub(int x)
{
    if (x>=65535)
    {
        printf("Please Enter the number smaller than 65535");
        return 0;
    }
    else
    {
        count=0;
        while (x!=0)
        {
            
        }
        
        for (int i = 0; i < 5; i++)
        {
            
        }
        
    }
    
}

int main()
{
    unsigned int x;
    printf("Enter the number: \t");
    scanf("%d",&x);
    add_sub(x);
    return 0;
}